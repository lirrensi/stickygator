import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
    appId: "io.ionic.stickygator",
    appName: "Stickygator",
    webDir: "dist",
};

export default config;
